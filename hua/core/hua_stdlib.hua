(require-macro hua.core.macros)
(require-macro hua.core.assignment)
(require-macro hua.core.module)

(defn apply [f args]
  (f (unpack args)))

(defn dec [n]
  (- n 1))

(defn first [tbl]
  (get tbl 1))

(defn inc [n]
  (+ n 1))

(export apply
        dec
        first
        inc)
